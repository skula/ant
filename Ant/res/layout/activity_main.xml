<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="horizontal"
    android:background="#ffffff">
 
	<ExpandableListView
		android:id="@+id/lvExp"
		android:layout_height="fill_parent"
		android:layout_width="wrap_content"
		android:groupIndicator="@null"/>

	<LinearLayout
		android:layout_height="wrap_content"
		android:layout_width="wrap_content"
		android:orientation="vertical">
	
		<!-- ICI: barre du chemin du dossier -->
		
		<LinearLayout
			android:layout_height="wrap_content"
			android:layout_width="wrap_content"
			android:orientation="horizontal">
			
			<!-- ICI: affichage de la liste (grille) dossiers et fichiers -->

			<!-- ICI: barre verticale des combinaisons (alt+f4, switch window, plein ecran ...) -->
		</LinearLayout>
		
		<!-- ICI: barre horizontale des operations sur dossier (couper/copier/coller, ouvrir/lancer, renommer ...) -->
		
	</LinearLayout>
 
</LinearLayout>